[[_TOC_]]

# Cassandra

## Repository structure

* `./charts` - the directory with HELM chart for Cassandra.
* `./Docs` - the directory with actual documentation for the service.
* `./version` - the directory with `description.yaml` and `build.sh` file for promotion process.
* `./Dockerfile` - the Dockerfile for Cassandra docker image.
* `./files` - the file for Cassandra run script and ssh config.